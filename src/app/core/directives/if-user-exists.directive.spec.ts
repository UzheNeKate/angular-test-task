import { IfUserExistsDirective } from './if-user-exists.directive';

describe('IfUserExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new IfUserExistsDirective();
    expect(directive).toBeTruthy();
  });
});
